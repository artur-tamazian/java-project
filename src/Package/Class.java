package Package;

import Import.Import;
import static Import.Import.Static;



/**
 * Class that extends Extends and implements Implements
 * @author author
 */
public abstract class Class<Super extends Extends> extends Extends<Extends<Extends>> implements Implements {

	/**
	 * returns returns
	 * @param param pam
	 * @return return
	 **/
	<Return> Return Return(Return Return) {
		final Final Final;
		boolean If, Else;
		if (If = true) Static(); else Else=If;
		Return.equals(Return);
		Import Export = Import.Export;
		Export.Import(Export);
		return Return(Return);
	}


	abstract <Abstract> Abstract Abstract(Abstract Abstract);
}
